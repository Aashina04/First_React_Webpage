//react functional component
import React from "react";

//function name should be same as file name
function Header(){
    return(
    <div>
        <h1>Header</h1>
    </div>
    );

 }
 export default Header;
 //we need to export the file as the main entry point for the webpage is not header.jsx but it is main.jsx